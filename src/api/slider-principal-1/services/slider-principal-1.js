'use strict';

/**
 * slider-principal-1 service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::slider-principal-1.slider-principal-1');
